-- Test Database Connection
-- Use this to verify your Railway MySQL database is accessible

-- Create tables if they don't exist (Hibernate will do this automatically)
-- But you can test the connection manually first

-- Test query
SELECT 'Database connection successful!' AS status;

-- Show databases
SHOW DATABASES;

-- Show tables (after Hibernate creates them)
SHOW TABLES;
